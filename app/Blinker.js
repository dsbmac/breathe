import React, { Component } from 'react';
import { Text } from 'react-native';
import Blink from './Blink';

export default class Blinker extends Component {
    render() {
        return (
            <Blink text="Suck me very dry"/>
        );
    }
}